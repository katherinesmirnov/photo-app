services:
  app:
    build: .
    image: photo-app
    ports:
      - "3000:3000"
    volumes:
      - ${HOST_DATA_DIR:-./data}:/app/data
      - ${HOST_UPLOADS_DIR:-./storage/uploads}:/app/storage/uploads
    environment:
      DATABASE_URL: "file:./data/prod.db"
